name: "Action"
on:
  pull_request:
    branches:
      - master
    paths:
      - 'vars/project-1/c1.txt'
  push:
    branches:
      - master
    paths:
      - 'vars/project-1/c1.txt'
jobs:
  call-workflow:
    uses: ./.github/workflows/template/called.yml
    with:
      Project_Id: project-1
      Name: SILVER
    secrets: inherit
    #Credentials: ${{ secrets.GOOGLE_CREDENTIALS }}

